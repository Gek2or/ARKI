# ARKI — Guided Nordic 1.8

**Статус: репозиторий подготовлен к импорту основной версии 1.8.**

ARKI 1.8 связывает существующие функции в один первый запуск: **профиль → магазины рядом → дни готовки → бюджет → пищевые предпочтения → техника/время → домашние запасы → готовое меню**. После мастера выбранные магазины участвуют в существующем сравнении корзины/поездки, а ужины попадают только на выбранные дни.

## Загрузить весь проект одним файлом

1. Сохраните из чата файл `ARKI_Project_v1.8_git.bundle`.
2. В этом репозитории выберите **Add file → Upload files**, загрузите файл в корень и подтвердите коммит в `main`.
3. Workflow **Import ARKI project** автоматически проверит точный снимок, запустит `npm test` и `npm run test:web`, импортирует исходники и удалит bundle из рабочей версии дерева.

Импорт принимает только подготовленную копию:

```text
SHA-256: c069d4042dcb42dcde1739b907459c4223cd6c1bdceed229ffd516c8b9fe0c34
Source commit: d0a8652a261881c4a5e55f89e618309d2f005944
Source files: 193
```

## Что уже проверено локально

- существующий проект/backend: 228 проверок;
- актуальная логика клиента: 66/66;
- сборка/HTTP/CSP: 9/9;
- браузерная регрессия: 109/109;
- Nordic layout/languages: 140/140;
- guided onboarding: 24/24 на 390 и 1440 px.

## Границы

Это импорт исходников, не публикация production-сайта. Живые прайсы K-ryhmä, S-ryhmä и Lidl не подключены; встроенные цены являются демонстрационными. Возможный K-ryhmä pilot — цель, а не подтверждённое партнёрство. Nordic-клиент пока хранит данные в браузере, а сохранённый закрытый Node/SQLite пилот остаётся отдельным synthetic-only режимом без автоматической семейной синхронизации.
